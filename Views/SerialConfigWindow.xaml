<Window x:Class="Views.SerialConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="串口参数配置" Height="350" Width="300" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="80"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<TextBlock Text="端口号:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
		<ComboBox Grid.Row="0" Grid.Column="1" Margin="0,5" ItemsSource="{Binding AvailablePorts}" SelectedItem="{Binding SelectedPort}"/>

		<TextBlock Text="波特率:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
		<ComboBox Grid.Row="1" Grid.Column="1" Margin="0,5" ItemsSource="{Binding BaudRates}" SelectedItem="{Binding SelectedBaudRate}"/>

		<TextBlock Text="数据位:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
		<ComboBox Grid.Row="2" Grid.Column="1" Margin="0,5" ItemsSource="{Binding DataBitsList}" SelectedItem="{Binding SelectedDataBits}"/>

		<TextBlock Text="停止位:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
		<ComboBox Grid.Row="3" Grid.Column="1" Margin="0,5" ItemsSource="{Binding StopBitsList}" SelectedItem="{Binding SelectedStopBits}"/>

		<TextBlock Text="校验位:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
		<ComboBox Grid.Row="4" Grid.Column="1" Margin="0,5" ItemsSource="{Binding Parities}" SelectedItem="{Binding SelectedParity}"/>

		<StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
			<Button Content="打开串口" Style="{DynamicResource AccentButtonStyle}" Margin="0,0,10,0" FontWeight="Bold" Command="{Binding ConnectSerialCommand}"/>
			<Button Content="关闭串口" FontWeight="Bold" Command="{Binding DisconnectSerialCommand}"/>
		</StackPanel>
	</Grid>
</Window>